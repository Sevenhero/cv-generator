/** @type {import('next').NextConfig} */
const nextConfig = {
  // assetPrefix: "/profile/",
  // output: "export",
  // images: {
  //   // FIXME: image was missing
  //   unoptimized: true,
  // },
};

export default nextConfig;
